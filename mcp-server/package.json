{
  "name": "nightraven-memory-chain-mcp-server",
  "version": "0.1.0",
  "description": "MCP tools for NightRaven project memory — read, search, and append the connected chain",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "nightraven-memory-chain-mcp": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
