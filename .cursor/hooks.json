{
  "version": 1,
  "_comment": "NightRaven Phase 2 — soft hooks + Always Sync autosync (git pull/commit/push). Windows: PowerShell; Unix: run-hook.sh dispatcher. Touch 3 AFTER = session-stop last turn only (loop_limit: 1). Pause via .cursor/touch3.disabled. See docs/HOOKS_SETUP.md.",
  "hooks": {
    "sessionStart": [
      {
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .cursor/hooks/session-start.ps1",
        "timeout": 30
      }
    ],
    "stop": [
      {
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .cursor/hooks/session-stop.ps1",
        "timeout": 60,
        "loop_limit": 1
      }
    ],
    "afterFileEdit": [
      {
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .cursor/hooks/after-file-edit.ps1",
        "timeout": 5
      }
    ]
  }
}
